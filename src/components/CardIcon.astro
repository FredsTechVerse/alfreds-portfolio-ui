---
interface Props {
    icon:any;
}
import {Image} from "astro:assets"
const {icon}=Astro.props
---
    <Image src={icon} alt="Alternative image"  class="w-8 aspect-square rounded-full flex flex-col items-center justify-center bg-blue-300/10 p-1 "/>
