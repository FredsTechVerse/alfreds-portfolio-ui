---
interface Props {
    icon:any;
}
import {Image} from "astro:assets"
const {icon}=Astro.props
---
    <Image src={icon} alt="Alternative image"  class="w-8 aspect-square rounded-lg flex flex-col items-center justify-center bg-slate-800 p-1 my-1 "/>
